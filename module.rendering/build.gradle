evaluationDependsOn(':anvillib-rendering-neoforge-26.1:renderdoc-loader-internal')

dependencies {
}

tasks.configureEach {
    if (name == 'runClientRenderDoc') {
        dependsOn ':anvillib-rendering-neoforge-26.1:renderdoc-loader-internal:jar'
    }
}
