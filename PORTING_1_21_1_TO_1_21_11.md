# 1.21.1 → 1.21.11 特性对照与移植记录

来源：本地 `fff/1.21.1`，`fe26ea6`（2026-09-12 核对）。目标：`fff/1.21.11-port`，起点 `fd30da6`。两者共同祖先为 `81c5a9c`。

目标版本保持 Minecraft **1.21.11**、NeoForge **21.11.40-beta**、Java **21**。结合提交历史、完整模块树、公开方法和资源清单比较；现代模型与 GUI 接入复用了本地 `fff/26.1-port` 的对应实现，再按 1.21.11 源码和实际运行结果适配。

## 逐模块对照

| 模块 | 移植结果 |
| --- | --- |
| Codec | 迁入 1–16 参数组合、条件流编解码、零/一/多元素列表、MapCodec 编码和标签键工具；保留新版组件谓词与注册表编码。 |
| Collision | 新增 AABB、三角形和三角形之间的碰撞检测及相关数学工具。 |
| Config | 补齐配置分组、嵌套对象、枚举翻译、点分隔配置键及格式化处理。 |
| Cube | 新增完整精确模型拾取与斜棱线模块：凸几何、BVH、轮廓缓存、父模型、旋转/镜像、随机变体、multipart、动态部件、邻格补扫、资源重载和运行时排除规则。包含最新仿射逆矩阵与双精度变换修复。 |
| Explosion | 新增分层球壳爆炸、熔化配方缓存、方块破坏和掉落处理。 |
| Font | 新增系统字体选择、SDF 字形图集与缓存、测量/布局、GUI 绘制和配置界面。 |
| Integration | 补齐分阶段集成和加载判断，数据生成接入适配 Client/Server 两类事件。 |
| Main | 聚合全部功能模块与同步处理器；本地项目依赖与发布依赖均导出 API，补齐发布环境声明。 |
| Moveable Entity Block | 保留活塞搬运、方块实体数据、回调与渲染状态；改用实际调用参数和类型序号捕获局部变量，消除依赖其他映射变量名造成的启动失败。 |
| Multiblock | 新增动态多方块、异步快照检查、未加载区块处理、形成/解体事件、控制器、配置和网络同步；保留公开定义序列化、BlockState 构建器和 formed 持久化。 |
| Network | 补齐 BoolAndInt、条件广播、公开协议和自动注册改进。 |
| Recipe | 迁入方块约束搜索树剪枝、匹配快照回滚、组件保存、缓存和物品输出修复；保留目标的事务式 ResourceHandler API，输出槽只同步差量并验证事务结果。 |
| Registrum | 补齐更多注册表构建器、能力注册、创造栏分区横幅、文字/提示、物品变体折叠、16 色面板、快捷栏/副手/丢弃/克隆操作；数据生成保持 1.21.11 模型、流体、配方和标签接口。恢复公开颠倒英语转换及物品栈原料工具。 |
| RPC | 新增远程方法注册、参数编码、返回值、失败/超时/断连传播、配置阶段协商与 invokeSync 系列同步阻塞调用。 |
| Space Select | 新增空间选区、滚轮调整、选区管理、事件、网络与客户端描边。 |
| Sync / processor | 新增代理字段、静态字段、惰性差分与配置同步；使用 1.21.11 已提供的 ClassProcessor SPI。修复扫描结果 EnumHolder 被错误转为字符串、导致 S2C/C2S 方向退回 BOTH 的问题。 |
| Util | 迁入工具库拆分、集合/组件/背包/形状/轮廓工具、加权方块状态、无限堆栈和客户端 tick 修复；组件子谓词使用新版 DataComponentMatchers，保留链式构建能力。 |
| Wheel | 迁入环扇、毛玻璃、动画、翻页、中心标题、方向标记、颜色定制和跨 0° 选区修复；使用 1.21.11 GUI 渲染状态。 |
| Test | 迁入来源测试示例，新增独立世界的模型、界面、配方、持久化、RPC、同步注入、字体、轮盘及活塞实际运行回归。 |
| CI / 文档 | 补齐模块矩阵、同步处理器构建顺序、Maven/模组发布、Roseau 工作流和中英文模块清单。 |

`Rendering` 是 Font/Wheel 共用的 GUI 支持模块，仅包含本次所需的 SDF、模糊和缓冲布局。来源分支已删除的 Yukkuri，以及 26.1 独有的 Bloom/Compute/缓存 BER 均不在移植范围内。

## 平台适配

- 模型接入使用 `SimpleModelWrapper`、`BlockModelPart`、`SimpleUnbakedGeometry`，准星入口是 `LocalPlayer.pick`，描边使用 `ExtractBlockOutlineRenderStateEvent`。
- 保留源版几何内存、状态数、部件数和后台任务限制；仅坐标乘加使用双精度，复用缓存矩阵。
- GUI 使用 `GuiGraphics`、`MouseButtonEvent`、`KeyEvent` 和 GPU RenderPipeline；离屏模糊关闭深度读写并释放临时纹理视图。
- 旧 `ItemSubPredicate` 对应新版组件匹配器；旧物品处理器缓存对应 `ResourceHandler<ItemResource>`；旧模型/配方数据生成提供器对应目标已有生成器。已被平台移除的旧 API 通过新版机制实现相同功能，不引入过期 Minecraft 类型。
- 多方块保存使用 SavedData Codec，保存名沿用 `anvillib_multiblocks`；formed 字段允许旧目标数据缺省。
- 客户端回归运行目录为 `module.test/build/port-1.21.11-smoke-run`，每次创建新世界，结果写入 `port-smoke-result.txt`，正常结束会保存并关闭集成服务器。

## 验证命令

```powershell
.\gradlew.bat build :anvillib-neoforge-1.21.11:generateMetadataFileForMavenJavaPublication --no-parallel --max-workers=1 '-ProseauBaselineVersion=2.0.0' --console=plain
.\gradlew.bat :anvillib-test-neoforge-1.21.11:runClient -PportSmoke --max-workers=3 '-ProseauBaselineVersion=2.0.0' --console=plain
.\gradlew.bat :anvillib-test-neoforge-1.21.11:runData --no-parallel --max-workers=1 '-ProseauBaselineVersion=2.0.0' --console=plain
git diff --check
```

`roseauBaselineVersion` 仅避免普通构建在配置阶段查询远程基线；上述命令不执行二进制兼容性报告或发布。

## 验证结果

- 最终全模块构建及 Maven 元数据生成通过，**251 个任务**。包含编译、资源、JAR、源码/Javadoc 包、ShapeUtil 和 std140 布局检查。
- Windows 客户端与集成服务器回归通过：`PORT_SMOKE_PASSED checks=327028`。
- 72 个实际原版方块状态完成模型捕获，几何占用 260,328 字节，无不支持状态；48 种旋转/镜像、161,376 条顶面射线、远坐标和 4,096 个随机种子核对通过。实际准星、高亮、排除规则和资源重载恢复均通过。
- 创造栏分区、完整 16 色面板、选择点击、松开消费、关闭、滚动和搜索保留通过。已查看创造栏、环扇轮盘和中英文字体三张截图；最终绘制无模糊管线深度缓冲警告。
- 真实粘性活塞推出并拉回箱子，保持同一方块实体、正确位置和十三颗钻石；同步代理实例/静态字段注入、方向注解、惰性登记及分组写回通过。
- 配方回滚、约束剪枝、重复/同优先级候选、拒绝插入的输出槽、多方块 formed 保存、无限堆栈和 RPC 返回值/异常/超时/断连回环检查通过。
- 测试模块 `runData` 通过，重新生成 1.21.11 的动态定义、掉落和语言资源；补齐新按键分类及环扇按键的中英文文本。
- 聚合 JAR 内含 **18 个模块和同步处理器**，742 个 AnvilLib 类的字节码版本均不超过 Java 21。发布坐标为 `dev.anvilcraft.lib:anvillib-neoforge-1.21.11:2.0.0`，18 个模块均导出到 API 元数据。
- CI 矩阵生成通过，共三层；同步处理器先于 Sync 构建，Space Select 位于 Network 之后。`git diff --check` 通过。
- 测试退出时玩家、区块及全部维度均完成保存。改动位于 `fff/1.21.11-port`，未发布。

其他平台、独立专用服务器、外部模组组合与真实多人连接未包含在本地回归中。
